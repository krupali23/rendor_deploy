services:
  - type: web
    name: kiez-connect-api
    env: python
    plan: free
    region: frankfurt           # or nearest to your users
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: KC_DATA_DIR
        value: /opt/render/project/src/backend/data